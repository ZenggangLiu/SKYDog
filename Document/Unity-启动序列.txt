UnityFramework:

1) UnityFramework::frameworkWarmup(argc, argv)
2) UnityFramnework::UnityInitRuntime(argc, argv)
    // iOS terminates open sockets when an application enters background mode.
    // The next write to any of such socket causes SIGPIPE signal being raised,
    // even if the request has been done from scripting side. This disables the
    // signal and allows Mono to throw a proper C# exception.
    std::signal(SIGPIPE, SIG_IGN);
3)  UIApplicationMain(
        argc, argv,
        nil, <--- 无Principle Class。即使用UIApplication
        [NSString stringWithUTF8String: AppControllerClassName]);
        使用"UnityAppController"作为UIApplication的Delegate。在UnityAppControll.h
4) UIApplicationDelegate:
   @interface UnityAppController : NSObject<UIApplicationDelegate>
   成员：
   - UnityView : UnityRenderingView : UIView
   - CADisplayLink